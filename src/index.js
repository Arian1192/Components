import React from 'react';
import ReactDOM from 'react-dom';
import Login from './components/login';
import BotonDarkTheme from './components/botonDarkTheme'
ReactDOM.render(
  <React.StrictMode>
    <BotonDarkTheme />
  </React.StrictMode>,
  document.getElementById('root')
);

